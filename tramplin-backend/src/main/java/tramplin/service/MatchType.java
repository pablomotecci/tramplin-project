package tramplin.service;

/**
 * Тип совпадения тега соискателя с требуемым тегом вакансии.
 * Используется для прозрачности скоринга: каждое совпадение объясняется
 * категорией (точное / синоним / общий родитель / иерархия).
 */
public enum MatchType {
    EXACT,
    SYNONYM,
    SIBLING,
    HIERARCHY,
    NONE
}
